快速开始：
安装依赖pip install -r requirements.txt
配置 config.py 中的 API Key（可选，仅 AI 功能需要）  

关于打包：
cmd输入venv\Scripts\activate进入虚拟环境之后
输入pyinstaller --windowed --icon="favicon.ico" --add-data "image;image" yuexinmiao.py
进行打包，生成文件在dist里，并且需要手动配备image图像资源


灵感来自网络热门桌宠“月星猫”
动画素材来源于网络（仅供学习交流，禁止倒卖收费）